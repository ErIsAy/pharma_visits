$('.calendar-main').fullCalendar('removeEvents', [<%= @event.id %>]);
$('.calendar-main').fullCalendar(
  'renderEvent',
  $.parseJSON("<%=j render(@event, format: :json).html_safe %>"),
  true
);
$('.modal').modal('hide');

-# %h1 Listing centers


.pusher
  %div.main.ui.container
    %h1.ui.header.blue Listado de Centros

    %table.ui.sortable.celled.table.striped
      %thead
        %tr
          %th Centro
          %th Dirección
          %th Ciudad
          %th
          %th
          %th

      %tbody
        - @centers.each do |center|
          %tr
            %td= center.name
            %td= center.address
            %td= center.city
            %td= link_to 'Ver', center, class: "ui brown basic submit button"
            %td= link_to 'Editar', edit_center_path(center), class: "ui blue basic submit button"
            %td= link_to 'Eliminar', center, :method => :delete,
                                            class: "ui red basic submit button",
                                            :data => { :confirm => '¿Está seguro…?' }

    %br

    -# = link_to 'New Center', new_center_path
    = link_to new_center_path, class: "ui blue basic submit button" do
      %i.big.icons
        %i.hospital.icon
        %i.corner.add.icon
      = 'Nuevo Centro'
